[package]
name = "as_cast"
version = "0.1.0"
edition = "2021"

[lints.rust]
# На данный момент мы отключаем предупреждения о неработающем коде, чтобы уменьшить
# шум компилятора.
# Мы снова включим их, как только объясним, как работает видимость в Rust.
dead_code = "allow"
